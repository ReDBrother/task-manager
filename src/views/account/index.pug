extends /layouts/application.pug

block content
  .row
    .col-lg-7.col-12
      +formFor(f, { name: 'Information', url: urlFor('account'), method: 'post' })
        input(type='hidden' name='_method' value='patch')
        +input(f, 'firstName')
        +input(f, 'lastName')
        +input(f, 'email')
        +button(f)
      .text-center
        a(href=urlFor('password')) Change password
    include /account/sidebar.pug


extends /layouts/application.pug

block content
  +formFor(f, { name: 'Sign Up', url: urlFor('users'), method: 'post' })
    +input(f, 'email')
    +input(f, 'firstName')
    +input(f, 'lastName')
    +input(f, 'password', { as: 'password' })
    +button(f)
